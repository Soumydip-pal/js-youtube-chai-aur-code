"use strict"; // treat all JS code as newer version

// alert(3 + 3) // we are using node.js not browser engine so, it will not work

/*Datatypes--- (1) primitive
                   number-> 2 to power 53
                   bigint
                   string->""
                   boolean->true/false
                   null-> standalonr value, representation of empty value
                   undefined->value not assigned
                   symbol->to identify uniqueness
                
                   (2) Non-primitive
                      objects->
                       */

                       console.log(typeof undefined); //undefined
                       console.log(typeof null);//object